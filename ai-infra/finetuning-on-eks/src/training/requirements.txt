# Core ML libraries
# torch is installed separately in Dockerfile with specific version
transformers>=4.51.0,<5.0.0  # Pin to 4.x: transformers 5.x eagerly materializes weights to GPU in bf16 before quantization, causing OOM for large QLoRA models. >=4.51 for Qwen3 MoE support
datasets>=3.0.0
accelerate>=1.0.0

# SFT and LoRA
trl>=0.15.0
peft>=0.14.0

# Quantization (QLoRA)
bitsandbytes>=0.45.0
triton>=3.0.0

# Training utilities
tokenizers>=0.15.0
safetensors>=0.4.0
sentencepiece>=0.1.99

# Flash Attention (optional, for faster training)
# Uncomment if building with CUDA support
# flash-attn>=2.5.0

# Monitoring and logging
tensorboard>=2.15.0
wandb>=0.16.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0

# AWS integration
boto3>=1.34.0
s3fs>=2024.1.0

# Evaluation
evaluate>=0.4.0
